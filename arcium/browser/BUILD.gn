# Browser-process services for the Arcium model: persistence, binding to live
# tabs, archiving and search. Depends on the model but not on Views.
source_set("browser") {
  sources = [
    "archive_store.cc",
    "archive_store.h",
    "arcium_profile_state.cc",
    "arcium_profile_state.h",
    "entry_claim.cc",
    "entry_claim.h",
    "model_store.cc",
    "model_store.h",
    "tab_binding.cc",
    "tab_binding.h",
  ]
  public_deps = [
    "//arcium/browser/model",
    "//components/tabs:public",
    "//sql",
    "//url",
  ]
  deps = [
    "//base",
    "//base:i18n",

    # BrowserContext only, for the SupportsUserData key. No //chrome deps.
    "//content/public/browser",
  ]
}
