# Perf: stage5b (2026-09-24)

Config `dev`, Chromium `152.0.7977.83`, arcium `47b7cf7`, 3 runs, 60 s idle, fresh profile, about:blank.
Method: see scripts/perf. Numbers are medians.

| Metric | Median |
|---|---|
| Startup to DevTools endpoint (ms) | 3617 |
| Idle RSS, all processes (MB) | 1373 |
| Process count | 10 |

Raw runs: `[{"startup_ms": 8382, "idle_rss_mb": 1452, "process_count": 10}, {"startup_ms": 3501, "idle_rss_mb": 1367, "process_count": 9}, {"startup_ms": 3617, "idle_rss_mb": 1373, "process_count": 10}]`

## Against the stages before it

| Metric | Stage 3b (2026-09-12) | Stage 4 (2026-09-16) | Stage 5a (2026-09-22) | Stage 5b (2026-09-24) |
|---|---|---|---|---|
| Startup to DevTools endpoint (ms) | 2988 | void (load 101) | 2927 | 3617 |
| Idle RSS, all processes (MB) | 1482 | 1459 | 1464 | 1373 |
| Process count | 9 | 9 | 9 | 10 |

Idle memory is 91 MB under Stage 5a, which is noise rather than a saving:
Stage 4's own three runs spread from 1384 to 1481. Nothing in this stage
allocates at idle -- the split row, the handle and the strip beside the page
are drawn only for a split or during a drag, and none is on screen here.

Two of the three runs counted 10 processes where every stage before had 9.
It did not come back: the same launch, listed by kind after the same minute
idle, twice more, gave 9 each time -- the browser, the GPU process, the
network and storage services, three renderers and two helpers without a
type. Everything this stage added is Views code in the browser process, so
the likeliest reading is a short-lived utility process that had not yet
exited, but what the tenth was is not known.

Startup is 690 ms slower than Stage 5a's median. The 1-minute load average
was 16 when the runs began, against 5 by the end of Stage 5a's, and the first
run's 8382 ms is again a cold start straight after a rebuild. This stage adds
nothing before the first paint, so the number is read as load until a quieter
run says otherwise.
